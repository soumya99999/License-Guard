export interface LicenseFormData {
  licenseId: string;
  type: string;
  vendor: string;
  validityStart: string;
  validityEnd: string;
  purchaseDate: string;
  maxUsers: number;
  invoiceNumber: string;
}
